services:
  redis:
    image: redis:7.4-alpine
    container_name: yczh_redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  mysql:
    image: mysql:8.0.39
    container_name: yczh_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root_dev
      MYSQL_DATABASE: yunchuan_zhihui
      MYSQL_USER: yunchuan
      MYSQL_PASSWORD: yunchuan_dev
    ports:
      # Keep MySQL's container port at 3306; expose a less collision-prone host port.
      - "${MYSQL_HOST_PORT:-13306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

  chromadb:
    image: chromadb/chroma:1.5.8
    container_name: yczh_chromadb
    environment:
      IS_PERSISTENT: "TRUE"
      PERSIST_DIRECTORY: /chroma/chroma
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma

  backend:
    build:
      context: ./backend
    container_name: yczh_backend
    env_file:
      - .env
    environment:
      DATABASE_URL: mysql+pymysql://yunchuan:yunchuan_dev@mysql:3306/yunchuan_zhihui
      CHROMA_HOST: chromadb
      CHROMA_PORT: 8000
      REDIS_URL: redis://redis:6379/0
      OPENAI_API_BASE: ${OPENAI_API_BASE:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      PRIMARY_LLM_MODEL: ${PRIMARY_LLM_MODEL:-}
      PRIMARY_REVIEW_MODEL: ${PRIMARY_REVIEW_MODEL:-}
      SECONDARY_REVIEW_MODEL: ${SECONDARY_REVIEW_MODEL:-}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-}
      ALLOW_FIXTURE_LLM: ${ALLOW_FIXTURE_LLM:-false}
      ENABLE_EVALUATION_OVERRIDES: ${ENABLE_EVALUATION_OVERRIDES:-false}
      ENABLE_EVALUATION_RUNNER: ${ENABLE_EVALUATION_RUNNER:-false}
      ENABLE_KNOWLEDGE_IMPORT_MODELS: ${ENABLE_KNOWLEDGE_IMPORT_MODELS:-true}
      KNOWLEDGE_IMPORT_MODEL_CONCURRENCY: ${KNOWLEDGE_IMPORT_MODEL_CONCURRENCY:-4}
      KNOWLEDGE_IMPORT_GENERATION_CONCURRENCY: ${KNOWLEDGE_IMPORT_GENERATION_CONCURRENCY:-3}
      KNOWLEDGE_IMPORT_REVIEW_CONCURRENCY: ${KNOWLEDGE_IMPORT_REVIEW_CONCURRENCY:-2}
      KNOWLEDGE_IMPORT_BATCH_TARGET_TOKENS: ${KNOWLEDGE_IMPORT_BATCH_TARGET_TOKENS:-8000}
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./data:/app/data
      - ./docs:/app/docs:ro
      - ./storage:/app/storage
      - candidate_index_state:/app/storage/candidate-index
      - ./reports:/app/reports
      - ./test_script:/app/test_script:ro
    depends_on:
      mysql:
        condition: service_healthy
      chromadb:
        condition: service_started
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health', timeout=3)"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s

  frontend:
    build:
      context: ./frontend
    container_name: yczh_frontend
    environment:
      VITE_API_BASE_URL: http://localhost:8000/api/v1
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app/frontend
      - frontend_node_modules:/app/frontend/node_modules
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:5173/"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s

volumes:
  mysql_data:
  chroma_data:
  frontend_node_modules:
  candidate_index_state:
  redis_data:
