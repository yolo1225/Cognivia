# 人工智能应用开发实训多智能体个性化知识生成系统设计文档

> 文档版本：v2.0
> 最近同步：2026 年 8 月 29 日
> 活动 Agent 契约：`agent-contract-v10`
> 主验证领域：`ai_app_dev`（人工智能应用开发实训）
> 实现事实源：可执行代码、V10 Pydantic 契约和自动化测试

## 1. 产品定位

Cognivia 是面向比赛演示的领域知识个性化生成与多智能体协同决策原型。当前目标不是建设完整
SaaS，而是稳定展示并量化以下闭环：

```text
学习者建档 -> 正式诊断 -> 学情画像 -> 学习路径
-> Candidate RAG 检索 -> 三类资源生成 -> 双模型审核
-> 自动定向修订 -> 整包原子发布 -> 反馈与学习证据更新
```

画像决定教学目标、目标难度、顺序和表达方式；检索证据决定专业事实边界；生成 Agent 负责把
证据组织成个性化资源；审核 Agent 是事实支持度与发布指标的唯一裁决入口。

## 2. 当前技术架构

### 2.1 技术栈

- 前端：Vue 3、TypeScript、Vite、Pinia、Axios、ECharts、Vue Flow。
- 后端：FastAPI、Python 3.12、SQLAlchemy、Alembic。
- 编排：LangGraph `StateGraph`，MySQL Checkpointer。
- 数据：MySQL 8 保存业务与运行记录，ChromaDB 保存 Candidate 向量索引。
- 模型：OpenAI-compatible API；生成模型和两个独立审核模型。
- 实时状态：SSE。
- 评测：离线 `test_script`。

### 2.2 服务边界

```text
Vue 前端
  -> /api/v1 REST + SSE
FastAPI API
  -> generation_worker
  -> LangGraph Agent runtime
  -> MySQL / ChromaDB / 模型网关
```

MySQL 保存用户、学习者、诊断、画像、路径、生成任务、Agent Run、消息摘要、资源、审核报告、
反馈、导学会话、错题和知识导入状态。Redis 只承载 refresh session 等运行支撑，不保存 Agent
State。ChromaDB 只使用已发布领域的 active Candidate collection。

认证采用 learner/admin、bcrypt 密码、JWT HttpOnly Cookie、CSRF Header 和 Redis refresh
session。所有学习者数据接口执行对象归属校验。

## 3. Agent 主工作流

唯一顶层构建入口是 `backend/app/agents/graphs.py::build_learning_graph()`：

```text
START -> prepare_task
prepare_task -> analyze_profile       初次生成
prepare_task -> interpret_feedback    资源反馈
interpret_feedback -> analyze_profile
analyze_profile -> finalize_task      无需生成或复核
analyze_profile -> retrieve_knowledge 需要生成、复核或修订
retrieve_knowledge -> generate_resource
generate_resource -> review_resource
review_resource -> finalize_task
finalize_task -> retrieve_knowledge   revision_required，最多两轮
finalize_task -> END                  completed/no_change/failed/rejected
```

六个 Agent 的职责如下：

1. Orchestrator Agent：路由任务、汇总审核结果、生成修订计划、决定原子发布或失败。
2. Profile Analysis Agent：基于诊断和正式学习证据更新画像，确定目标难度、知识点与资源分配。
3. Knowledge Retrieval Agent：查询 active Candidate index，返回可追溯 Chunk 与正式题目候选。
4. Content Generation Agent：按画像和检索证据生成稳定结构化资源，不决定事实是否最终通过。
5. Review and Validation Agent：双模型检查专业声明，计算事实、难度和覆盖指标，必要时补检索。
6. Tutoring Agent：理解自然语言反馈并决定追问、解释、复核、补救或挑战任务。

每个生成事件使用 `generation_tasks.public_id` 作为公开 `task_id` 与 LangGraph `thread_id`。
反馈、知识刷新和学习调整任务通过来源 ID 建立继承关系，不复用旧任务身份。

## 4. Candidate RAG

### 4.1 检索输入

`RetrieveKnowledgeInput` 包含：

- 任务上下文：领域、学习目标、资源类型和触发类型；
- 学习者画像与薄弱知识；
- 检索计划：优先知识点、前置知识点、关键词、难度、Top-K；
- 可选修订计划：问题代码、缺失知识点和补检索关键词；
- 检索目的：初次生成或修订。

### 4.2 检索过程

```text
显式知识点召回
+ 知识关系扩展
+ 向量语义召回
+ 难度与画像排序
+ 资源证据能力增强
```

实训指南优先为同一目标选择 `operation + expected_result` 证据。能力标签只用于排序、诊断和
知识治理；缺少某种能力时返回 warning，不直接终止生成。概念证据仍可生成比较、分析、设计、
记录和评审活动。

### 4.3 检索输出

`RetrieveKnowledgeOutput` 返回最终查询文本、最多 18 个 `RetrievedChunk`、已覆盖和缺失知识点、
诊断 warnings，以及正式题库候选。每个 Chunk 带知识点、难度、相似度、匹配方式、来源定位和
内容校验信息。

索引清单缺失、活动集合不可用、索引元数据不一致、Embedding 失败或向量维度错误属于检索硬
错误。单个知识点未召回或能力不完整属于软缺口。

## 5. 资源生成

### 5.1 输入与资源类型

`GenerateResourceInput` 包含画像、检索 Chunk、认证题目候选、当前路径节点和
`GenerationRequirements`。要求中明确资源类型、难度、知识点分配、来源白名单、个性化说明和
可选字段级修订计划。

系统生成：

- `lecture`：学习目标、核心概念、误区和总结；
- `practice_guide`：环境条件、步骤、可选命令、可观察结果和验收动作；
- `graded_quiz`：从 active 正式题库确定性组卷，3–8 题。先使用目标难度 `±2`
  内的题目，不足 3 题时仅放宽难度并按距离补足；定向修订保留已通过题目，只替换审核拒绝的位置。

### 5.2 生成边界

生成 Agent 每种资源只走一条主路径：正常生成或审核后的字段级定向修订。生成后仅执行：

- V10 Schema 校验；
- 来源白名单校验，非法来源最多定向修复一次；
- 确定性的元话术、空白可选字段和文本结构规范化；
- Markdown 渲染、来源映射、知识覆盖映射和审核声明清单构建。

生成阶段不再执行内容策略模型修复、覆盖补写或证据能力硬裁决。目标证据缺失和生成覆盖不足
记录 warning 后进入审核。没有固定技术结果证据时，`expected_result` 可以是检查表、对比报告、
设计稿、记录或核对结论等学习产物。

### 5.3 生成输出与硬失败

`GenerateResourceOutput` 返回 1–3 个 `GeneratedResourceArtifact`。每份资源包含结构化内容、
Markdown、难度、精确来源、知识覆盖和原子审核声明。

仅以下问题使生成失败：

- 输入、Schema 或资源类型无法构造；
- 模型调用或结构化输出重试后仍失败；
- 无任何合法来源或来源修复后仍越过白名单；
- 正式题库不足最低题量或认证题目蓝图失效；
- 资源没有可审核的专业声明；
- 审核修订补丁非法且无法从原候选恢复；
- 某个并行资源单独重试后仍失败。

并行资源分别收集结果，只重试失败类型；成功资源不重复生成。只有所有请求资源有效后才构造
完整输出，系统不部分发布。

## 6. 双模型审核与补检索

审核输入是完整资源、生成要求和本次检索证据。讲义与实训指南先拆成带字段路径和来源的原子
专业声明，再由 `primary_review_model` 和 `secondary_review_model` 独立检查事实与来源。认证测试题
执行题库确定性校验。

以下任一情况触发一次针对受影响声明的补检索和双通道复核：

- 两个模型对声明、分数或通过结论存在分歧；
- 声明证据不足；
- 声明与证据明确矛盾；
- 目标知识点缺少被支持的专业声明。

补检索为空时继续使用原证据完成复核，不把空结果本身作为失败条件。最终声明状态分为
`supported`、`contradicted`、`evidence_insufficient` 和 `unresolved`。

资源和整包使用同一组官方门槛：

- 幻觉率 `< 5%`；
- 难度匹配率 `>= 85%`；
- 核心知识覆盖率 `>= 90%`。

矛盾、证据不足和未解决声明进入幻觉率分子，但不是独立的一票否决项。审核不通过时只修订受
影响资源和字段；无法获得证据的声明在修订时删除、弱化或改成非事实性的教学动作。达到两轮
修订上限仍不达标时整包失败且不发布。

## 7. 知识导入与正式题库

管理员通过文档驱动批次完成上传、候选生成、确定性校验、图谱预览、Candidate 索引构建、检索
冒烟和确认发布。发布使用 staging 到 active 的事务切换，失败批次不会污染学习者检索。

知识关系保存在 MySQL，ChromaDB 不承担图数据库职责。关系用于前置路径、关联召回和影响分析。
知识更新后标记待重嵌入，并通过关系定位受影响路径和资源。

正式题目只绑定主知识点和可选关联知识点；导入只进行模板、目录、槽位、结构、答案、解析与唯一用途的确定性校验。题目生命周期使用 `active/staged/stale/disabled`，活动题通过 `answer_key_json.question_bank_uses` 声明唯一用途：

- `diagnosis`：首次诊断固定抽取 10 题（6 道单选、4 道简答）；
- `graded_quiz`：分阶测验确定性组卷，每包至少 3 题；标准难度题不足时允许使用最近难度的合法题补足，
  难度兜底不得替代唯一用途、知识范围、运行时来源证据和结构门禁；
- `mastery_validation`：独立掌握检查，区分基础检查和迁移检查并排除已见题。

三个用途互斥，所有运行入口只使用 `active` 题目。错题巩固不是题目用途：首次诊断和
分阶测验答错后记录原题 ID，巩固时直接重做原题。`mistake_correction` 参与画像分析并在通过后
关闭错题项，但不计入正式掌握证据。路线推进必须同时满足当前节点错题全部解决、分阶测验完成、
每个核心知识点独立掌握证据充足。掌握检查答错不创建错题项，由后续未见掌握检查题继续验证。

知识 Chunk 通过 Candidate/Chroma 索引召回；正式题目在确定知识范围后直接从 MySQL 活动题库
筛选，并以题干、答案和解析动态检索当次 Chunk 作为生成与审核证据。仅修改题目用途、状态或数量不要求重建向量索引，知识正文、来源 Chunk 或 embedding 配置
变化时才重建 Candidate 索引。

## 8. 持久化、可观测性与发布

- 每个节点保存结构化输入输出和脱敏 Agent Run 摘要。
- 普通日志只记录 ID、状态、计数、分数、错误码和字段路径，不记录完整画像或资源正文。
- SSE 发布任务路由、节点状态、审核分歧、补检索、修订和终态事件。
- 学习者页面展示业务结果；完整 Agent payload 只通过受控 API 和离线评测访问。
- Orchestrator 只在完整资源集合和整包指标通过时原子发布。

## 9. 当前边界

MVP 当前不建设企业级身份生命周期、通用分布式任务队列、Neo4j、WebSocket、在线人工审核工作台、
学习主图中的人工暂停节点、`/api/v2` 或长期多版本兼容体系。历史任务按数据库中的原契约版本保留，
但旧契约资料不再作为开发输入，新任务只运行 V10。

## 10. 验证入口

```powershell
docker compose exec backend python -m ruff check app tests
docker compose exec backend python -m pytest -q
docker compose exec backend python -m app.scripts.evaluate_rag --split development --live --json
```

离线 `test_script` 是比赛指标事实来源。真实模型评测必须绑定案例集、模型、知识版本和 Candidate
索引，不得把 fixture baseline 当作当前真实模型成绩。
