# 设计文档索引

根目录保留完整设计文档：

- `设计文档-人工智能应用开发实训多智能体个性化知识生成系统.md`

该文档已经收敛为当前 V10 实现说明，不再包含旧契约草图或未实现 API。当前开发请同时阅读：

- `docs/current-iteration-plan.md`：根据当前已完成内容调整后的迭代计划。
- `docs/project-conventions.md`：面向开发代理的执行规范、前后端约定、知识库和 Agent 规则。

优先级说明：

1. 根目录需求文档决定产品目标和评分优先级。
2. 根目录设计文档决定架构边界和技术栈。
3. `docs/current-iteration-plan.md` 决定当前交付顺序。
4. `docs/project-conventions.md` 决定开发代理的日常执行规范。

旧 Agent 版本、生成期事实修复或人工暂停节点的资料不构成当前设计依据。
