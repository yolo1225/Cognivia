# 比赛要求复核清单

复核日期：2026-08-19。当前状态是“V6实现、50案例真实模型正式评测和10次真实模型稳定性验收均已通过”。

| 比赛要求 | 当前实现与可复现证据 | 验收状态 |
|---|---|---|
| 至少3个职责明确的Agent | 6个独立Agent、独立Prompt、结构化输入输出、Agent Run及标准消息 | 已实现；正式运行抽查 |
| 画像→检索→生成→审核→决策闭环 | 唯一 `build_learning_graph()`，首次生成与反馈共用任务/线程语义 | 已实现并完成正式评测 |
| 至少3种资源 | `lecture`、`practice_guide`、`graded_quiz` | 已实现；稳定性脚本校验恰好3类 |
| 反馈动态更新 | `feedback_revision` 复用原包目标，只生成受影响资源并继承另外两类 | 已实现；5个正式案例覆盖 |
| 挑战任务 | `challenge_task` 经导学会话触发，不由快捷反馈直接改画像；画像未变化时保留原资源并以 `completed/no_change` 结束 | 已实现；5个正式案例覆盖且均可判定 |
| 至少3组差异化画像 | beginner、intermediate、advanced | 50案例均衡覆盖并由评测脚本强校验 |
| 至少50个真实案例 | `data/evaluation_cases/v4/p0_cases.json`：40首次生成、5反馈修订、5挑战任务 | 数据与校验已完成 |
| 幻觉率 `<5%` | V6仅以 `contradicted + unresolved` 为分子；两者及证据不足均阻止受影响资源发布 | 正式live：1/591，0.17%，通过 |
| 难度匹配 `>=85%` | 单资源按分配目标审核；未生成新资源的 `no_change` 不进入资源难度分母 | 正式live：45/45，100%，通过 |
| 核心知识覆盖 `>=90%` | 按最终学习包唯一主目标统计，测验目标不扩大分母；`no_change` 不虚构覆盖分母 | 正式live：90/90，100%，通过 |
| 双模型审核与仲裁 | 分差大于10分或通过结论冲突时重新检索、复核和仲裁 | 已实现并有策略/集成测试 |
| 自动局部修订 | 类型化字段补丁、允许路径限制、单调性保护、最多2轮 | 已实现并有补丁/恢复测试 |
| 可恢复运行 | 1/3/5秒最多3次重试，持久化检查点自动恢复一次 | 已实现；10/10真实模型稳定性通过 |
| 可复现评测 | 运行记录绑定完整案例集、阶段案例集、模型、知识版本和RAG索引 | 已实现；旧报告不能作为新阶段凭据 |
| 协作与学习结果可视化 | 保留项目现有前端能力 | 本次不改前端、不新增审核界面或流程图 |

## 正式验收证据

- 正式真实模型运行 `live-formal-20260819T014223Z`：50/50案例可判定，幻觉率0.17%，难度匹配100%，核心覆盖100%，比赛门槛全部通过。
- 唯一资源级异常是 `V4-EVAL-037`：两轮修订后仍有1条证据不足和1条未解决声明，资源正确隔离；按总体比赛指标统计后仍通过，不要求每个诊断项逐字完美。
- 稳定性运行 `stability-20260819T043919Z`：5次完整生成和5次局部刷新全部成功；所有包的幻觉、证据不足和未解决数均为0，难度与覆盖均为100%，无人工恢复。
- 第46至50案例依据需求F2.4.6验证画像未变化分支：保留原画像、路径和资源，`completed/no_change` 是有效可判定终态，难度和覆盖对新资源不适用。
- `reports/evaluation/latest-baseline.*` 仍仅作为50/50 fixture合同基线，不替代上述真实模型成绩。
- Chroma 增量重建已完成：50个知识条目、109个分块、无待重嵌入标记，`rag.ready=true`；索引版本为 `sha256:3a6b2f4289f340e54fa6fc300c47d0e709bda33e6bb4c924fb4b7e4a5dcb613e`。
- 真实模型配置：生成 `qwen-plus`、主审核 `qwen-max`、副审核 `qwen-plus`；RAG索引与知识版本均绑定在报告中。

证据不足不视为事实错误，但仍属于发布阻断项。这个门禁不能通过降低规则强度来绕过；应修复检索程序或补齐知识证据。
