# M1 自动化测试记录

日期：2026-08-19

## 结果

- M1 聚焦测试：`9 passed`。
- Candidate 索引、manifest、RAG 准入和领域 API 回归：`26 passed`。
- 后端全量回归：`438 passed, 3 skipped`。
- 前端单元测试：`46 passed`。
- 前端生产构建：通过。

## 覆盖范围

- 多章节文档生成多个带来源候选；
- 文档重复上传隔离；
- 来源缺失、自环、前置关系环和错误选择题答案阻止批准；
- 部分批准必须包含关系或题目引用的知识候选；
- 批准候选事务写入知识、关系和诊断题；
- Candidate 索引、manifest、准入和领域隔离回归。

## 真实环境最终验收

- 验收文档：3 个 Markdown 章节；
- 候选：3 个知识点、2 条关系、3 道诊断题，`8 valid / 0 invalid`；
- Candidate 构建：增量模式，复用 109 个切片，重嵌入 3 个知识点；
- 活动索引：53 个知识点、112 个切片、1024 维真实 embedding；
- 名称查询：Top-1 命中本次导入知识；
- 释义查询：Top-1 命中本次导入知识；
- 发布：8 个候选全部发布，导入状态为 `ready`，RAG 状态为 `ready`。

验收中首次构建被诊断题来源定位门禁阻止，旧活动索引未切换。修复来源映射、失败恢复和真实检索冒烟门禁后，使用同一导入记录重试成功，证明失败不会污染活动索引。

结论：M1 完成，可以进入 M2。
